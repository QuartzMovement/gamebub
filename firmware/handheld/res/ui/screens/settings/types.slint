export enum SettingType {
    subpage,
    checkbox,
    datetime,
}

export struct SettingDatetime {
    year: int,
    month: int,
    day: int,
    hour: int,
    min: int,
    sec: int,
}

export struct SettingValue {
    bool-value: bool,
    datetime-value: SettingDatetime,
}

export struct SettingEntry {
    name: string,
    type: SettingType,
    value: SettingValue,
}